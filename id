local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

-- ===================== 白名单系统 =====================
local whitelist = {
    "KoaIaIala",
    "zero456127",
    "g88714033"
}

local function isWhitelisted(player)
    if not player then return false end
    local playerName = player.Name
    for _, whitelisted in ipairs(whitelist) do
        if whitelisted == playerName then
            return true
        end
    end
    return false
end

local function showWhitelistWarning()
    local whitelistGui = Instance.new("ScreenGui")
    whitelistGui.Name = "WhitelistGui"
    whitelistGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    whitelistGui.ResetOnSpawn = false
    whitelistGui.Parent = CoreGui

    local backgroundOverlay = Instance.new("Frame")
    backgroundOverlay.Size = UDim2.new(1, 0, 1, 0)
    backgroundOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    backgroundOverlay.BackgroundTransparency = 0.7
    backgroundOverlay.Parent = whitelistGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0, 450, 0, 250)
    mainFrame.Position = UDim2.new(0.5, -225, 0.5, -125)
    mainFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = whitelistGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = mainFrame

    local shadow = Instance.new("UIStroke")
    shadow.Color = Color3.fromRGB(0, 0, 0)
    shadow.Transparency = 0.6
    shadow.Thickness = 6
    shadow.Parent = mainFrame

    local topBar = Instance.new("Frame")
    topBar.Size = UDim2.new(1, 0, 0, 8)
    topBar.BackgroundColor3 = Color3.fromRGB(60, 120, 220)
    topBar.Parent = mainFrame

    local infoIcon = Instance.new("TextLabel")
    infoIcon.Size = UDim2.new(0, 60, 0, 60)
    infoIcon.Position = UDim2.new(0.5, -30, 0, 20)
    infoIcon.BackgroundColor3 = Color3.fromRGB(60, 120, 220)
    infoIcon.Text = "i"
    infoIcon.Font = Enum.Font.SourceSansBold
    infoIcon.TextSize = 40
    infoIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
    infoIcon.TextXAlignment = Enum.TextXAlignment.Center
    infoIcon.TextYAlignment = Enum.TextYAlignment.Center
    infoIcon.Parent = mainFrame
    local iconCorner = Instance.new("UICorner")
    iconCorner.CornerRadius = UDim.new(0.5, 0)
    iconCorner.Parent = infoIcon

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -40, 0, 30)
    titleLabel.Position = UDim2.new(0, 20, 0, 90)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "Need whitelist"
    titleLabel.Font = Enum.Font.SourceSansBold
    titleLabel.TextSize = 40
    titleLabel.TextColor3 = Color3.fromRGB(60, 120, 220)
    titleLabel.TextXAlignment = Enum.TextXAlignment.Center
    titleLabel.Parent = mainFrame

    local messageLabel = Instance.new("TextLabel")
    messageLabel.Size = UDim2.new(1, -40, 0, 60)
    messageLabel.Position = UDim2.new(0, 20, 0, 130)
    messageLabel.BackgroundTransparency = 1
    messageLabel.Text = "A whitelist is required to use this script. Please join my Discord group and submit your ID. discord.gg/kPN2Szg5E"
    messageLabel.Font = Enum.Font.SourceSans
    messageLabel.TextSize = 25
    messageLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    messageLabel.TextXAlignment = Enum.TextXAlignment.Center
    messageLabel.TextYAlignment = Enum.TextYAlignment.Top
    messageLabel.TextWrapped = true
    messageLabel.Parent = mainFrame

    local buttonContainer = Instance.new("Frame")
    buttonContainer.Size = UDim2.new(1, -40, 0, 40)
    buttonContainer.Position = UDim2.new(0, 20, 0, 190)
    buttonContainer.BackgroundTransparency = 1
    buttonContainer.Parent = mainFrame

    local copyButton = Instance.new("TextButton")
    copyButton.Size = UDim2.new(0.45, 0, 1, 0)
    copyButton.Position = UDim2.new(0, 0, 0, 0)
    copyButton.BackgroundColor3 = Color3.fromRGB(60, 120, 220)
    copyButton.Text = "Copy link"
    copyButton.Font = Enum.Font.SourceSansBold
    copyButton.TextSize = 35
    copyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyButton.Parent = buttonContainer
    local copyCorner = Instance.new("UICorner")
    copyCorner.CornerRadius = UDim.new(0, 6)
    copyCorner.Parent = copyButton

    local closeButton = Instance.new("TextButton")
    closeButton.Size = UDim2.new(0.45, 0, 1, 0)
    closeButton.Position = UDim2.new(0.55, 0, 0, 0)
    closeButton.BackgroundColor3 = Color3.fromRGB(50, 50, 58)
    closeButton.Text = "Close"
    closeButton.Font = Enum.Font.SourceSansBold
    closeButton.TextSize = 35
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.Parent = buttonContainer
    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 6)
    closeCorner.Parent = closeButton

    -- 动画
    mainFrame.Size = UDim2.new(0, 0, 0, 0)
    infoIcon.Size = UDim2.new(0, 0, 0, 0)
    titleLabel.TextTransparency = 1
    messageLabel.TextTransparency = 1
    copyButton.BackgroundTransparency = 1
    copyButton.TextTransparency = 1
    closeButton.BackgroundTransparency = 1
    closeButton.TextTransparency = 1

    TweenService:Create(backgroundOverlay, TweenInfo.new(0.3), {BackgroundTransparency = 0.7}):Play()
    TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 450, 0, 250)}):Play()

    task.wait(0.2)
    TweenService:Create(infoIcon, TweenInfo.new(0.3, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Size = UDim2.new(0, 60, 0, 60)}):Play()

    task.wait(0.1)
    TweenService:Create(titleLabel, TweenInfo.new(0.5), {TextTransparency = 0}):Play()
    task.wait(0.2)
    TweenService:Create(messageLabel, TweenInfo.new(0.5), {TextTransparency = 0}):Play()

    task.wait(0.2)
    TweenService:Create(copyButton, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
    TweenService:Create(copyButton, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
    TweenService:Create(closeButton, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
    TweenService:Create(closeButton, TweenInfo.new(0.3), {TextTransparency = 0}):Play()

    copyButton.MouseButton1Click:Connect(function()
        setclipboard("discord.gg/kPN2Szg5E")
        local copyLabel = Instance.new("TextLabel")
        copyLabel.Size = UDim2.new(1, 0, 1, 0)
        copyLabel.BackgroundTransparency = 1
        copyLabel.Text = "Link copied"
        copyLabel.Font = Enum.Font.SourceSansBold
        copyLabel.TextSize = 14
        copyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        copyLabel.TextXAlignment = Enum.TextXAlignment.Center
        copyLabel.TextYAlignment = Enum.TextYAlignment.Center
        copyLabel.TextTransparency = 1
        copyLabel.Parent = copyButton
        TweenService:Create(copyLabel, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
        task.wait(1)
        TweenService:Create(copyLabel, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
        task.wait(0.3)
        copyLabel:Destroy()
    end)

    closeButton.MouseButton1Click:Connect(function()
        TweenService:Create(backgroundOverlay, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        TweenService:Create(mainFrame, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        task.wait(0.3)
        whitelistGui:Destroy()
    end)
end

local function checkWhitelist()
    local localPlayer = Players.LocalPlayer
    if not localPlayer then return false end
    if not isWhitelisted(localPlayer) then
        print("[Whitelist System] Detected non-whitelisted user:", localPlayer.Name)
        showWhitelistWarning()
        return true
    end
    return false
end

if checkWhitelist() then
    return
end

local Events = ReplicatedStorage:WaitForChild("Events")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- ===================== 本地化UI库实现 (改进版) =====================
local UI = {}
UI.__index = UI

function UI:NewWindow(title)
    local self = setmetatable({}, UI)
    self.MainGui = Instance.new("ScreenGui")
    self.MainGui.Name = "ChiChiWorldUI"
    self.MainGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    self.MainGui.ResetOnSpawn = false
    self.MainGui.Parent = syn and CoreGui or PlayerGui

    self.Window = Instance.new("Frame")
    self.Window.Name = "MainWindow"
    self.Window.Size = UDim2.new(0, 280, 0, 200)   -- 更宽，初始高度略高
    self.Window.Position = UDim2.new(0.1, 0, 0.1, 0)
    self.Window.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
    self.Window.BorderSizePixel = 0
    self.Window.ClipsDescendants = true
    self.Window.Parent = self.MainGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = self.Window

    local shadow = Instance.new("UIStroke")
    shadow.Color = Color3.fromRGB(0, 0, 0)
    shadow.Transparency = 0.6
    shadow.Thickness = 4
    shadow.Parent = self.Window

    self.TitleBar = Instance.new("Frame")
    self.TitleBar.Name = "TitleBar"
    self.TitleBar.Size = UDim2.new(1, 0, 0, 38)
    self.TitleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
    self.TitleBar.Parent = self.Window

    -- 顶部装饰线
    local topLine = Instance.new("Frame")
    topLine.Size = UDim2.new(1, 0, 0, 2)
    topLine.BackgroundColor3 = Color3.fromRGB(60, 120, 220)
    topLine.Parent = self.TitleBar

    self.TitleText = Instance.new("TextLabel")
    self.TitleText.Size = UDim2.new(1, -40, 1, 0)
    self.TitleText.Position = UDim2.new(0, 12, 0, 0)
    self.TitleText.BackgroundTransparency = 1
    self.TitleText.Text = title
    self.TitleText.Font = Enum.Font.GothamBold
    self.TitleText.TextSize = 16
    self.TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
    self.TitleText.TextXAlignment = Enum.TextXAlignment.Left
    self.TitleText.Parent = self.TitleBar

    self.CollapseBtn = Instance.new("TextButton")
    self.CollapseBtn.Size = UDim2.new(0, 40, 1, 0)
    self.CollapseBtn.Position = UDim2.new(1, -40, 0, 0)
    self.CollapseBtn.BackgroundTransparency = 1
    self.CollapseBtn.Text = "-"
    self.CollapseBtn.Font = Enum.Font.GothamBold
    self.CollapseBtn.TextSize = 20
    self.CollapseBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    self.CollapseBtn.Parent = self.TitleBar

    self.ContentContainer = Instance.new("ScrollingFrame")
    self.ContentContainer.Size = UDim2.new(1, -8, 1, -38)
    self.ContentContainer.Position = UDim2.new(0, 4, 0, 38)
    self.ContentContainer.BackgroundTransparency = 1
    self.ContentContainer.ScrollBarThickness = 4
    self.ContentContainer.CanvasSize = UDim2.new(1, 0, 0, 0)
    self.ContentContainer.Parent = self.Window

    self:MakeDraggable()

    self.IsCollapsed = false
    self.CollapseBtn.MouseButton1Click:Connect(function()
        self:ToggleCollapse()
    end)

    self.Sections = {}
    self.CurrentY = 0
    return self
end

function UI:MakeDraggable()
    local dragging = false
    local dragStart = Vector2.new()
    local startPos = UDim2.new()
    self.TitleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = self.Window.Position
            UserInputService.MouseIconEnabled = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            self.Window.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
            UserInputService.MouseIconEnabled = true
        end
    end)
end

function UI:ToggleCollapse()
    self.IsCollapsed = not self.IsCollapsed
    if self.IsCollapsed then
        TweenService:Create(self.Window, TweenInfo.new(0.2), {Size = UDim2.new(0, 280, 0, 38)}):Play()
        self.CollapseBtn.Text = "+"
        self.ContentContainer.Visible = false
    else
        TweenService:Create(self.Window, TweenInfo.new(0.2), {Size = UDim2.new(0, 280, 0, 200)}):Play()
        self.CollapseBtn.Text = "-"
        task.wait(0.2)
        self.ContentContainer.Visible = true
    end
end

function UI:NewSection(title)
    local section = {}
    section.Title = title
    section.Frame = Instance.new("Frame")
    section.Frame.Size = UDim2.new(1, -12, 0, 44)
    section.Frame.Position = UDim2.new(0, 6, 0, self.CurrentY)
    section.Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
    section.Frame.Parent = self.ContentContainer

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = section.Frame

    section.TitleText = Instance.new("TextLabel")
    section.TitleText.Size = UDim2.new(1, -40, 1, 0)
    section.TitleText.Position = UDim2.new(0, 12, 0, 0)
    section.TitleText.BackgroundTransparency = 1
    section.TitleText.Text = title
    section.TitleText.Font = Enum.Font.GothamBold
    section.TitleText.TextSize = 15
    section.TitleText.TextColor3 = Color3.fromRGB(220, 220, 220)
    section.TitleText.TextXAlignment = Enum.TextXAlignment.Left
    section.TitleText.Parent = section.Frame

    section.ToggleBtn = Instance.new("TextButton")
    section.ToggleBtn.Size = UDim2.new(0, 40, 1, 0)
    section.ToggleBtn.Position = UDim2.new(1, -40, 0, 0)
    section.ToggleBtn.BackgroundTransparency = 1
    section.ToggleBtn.Text = "+"
    section.ToggleBtn.Font = Enum.Font.GothamBold
    section.ToggleBtn.TextSize = 18
    section.ToggleBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    section.ToggleBtn.Parent = section.Frame

    section.Content = Instance.new("Frame")
    section.Content.Size = UDim2.new(1, -12, 0, 0)
    section.Content.Position = UDim2.new(0, 6, 0, self.CurrentY + 44)
    section.Content.BackgroundTransparency = 1
    section.Content.ClipsDescendants = true
    section.Content.Parent = self.ContentContainer

    section.IsExpanded = false
    section.Buttons = {}
    section.ContentY = 0

    self.CurrentY = self.CurrentY + 44
    self.ContentContainer.CanvasSize = UDim2.new(1, 0, 0, self.CurrentY)

    local newHeight = math.max(200, 38 + self.CurrentY + 20)
    TweenService:Create(self.Window, TweenInfo.new(0.2), {Size = UDim2.new(0, 280, 0, newHeight)}):Play()

    section.ToggleBtn.MouseButton1Click:Connect(function()
        section.IsExpanded = not section.IsExpanded
        if section.IsExpanded then
            section.ToggleBtn.Text = "-"
            TweenService:Create(section.Content, TweenInfo.new(0.2), {Size = UDim2.new(1, -12, 0, section.ContentY)}):Play()
            self.CurrentY = self.CurrentY + section.ContentY
        else
            section.ToggleBtn.Text = "+"
            TweenService:Create(section.Content, TweenInfo.new(0.2), {Size = UDim2.new(1, -12, 0, 0)}):Play()
            self.CurrentY = self.CurrentY - section.ContentY
        end

        self.ContentContainer.CanvasSize = UDim2.new(1, 0, 0, self.CurrentY)
        local newHeight = math.max(200, 38 + self.CurrentY + 20)
        TweenService:Create(self.Window, TweenInfo.new(0.2), {Size = UDim2.new(0, 280, 0, newHeight)}):Play()
    end)

    -- 改进：支持默认开启状态
    function section:CreateToggle(text, callback, defaultState)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 42)
        btn.Position = UDim2.new(0, 0, 0, section.ContentY)
        btn.BackgroundColor3 = Color3.fromRGB(38, 38, 44)
        btn.Parent = section.Content

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 6)
        corner.Parent = btn

        local btnText = Instance.new("TextLabel")
        btnText.Size = UDim2.new(1, -50, 1, 0)
        btnText.Position = UDim2.new(0, 12, 0, 0)
        btnText.BackgroundTransparency = 1
        btnText.Text = text
        btnText.Font = Enum.Font.Gotham
        btnText.TextSize = 14
        btnText.TextColor3 = Color3.fromRGB(240, 240, 240)
        btnText.TextXAlignment = Enum.TextXAlignment.Left
        btnText.Parent = btn

        local toggleState = defaultState or false
        local stateLabel = Instance.new("TextLabel")
        stateLabel.Size = UDim2.new(0, 40, 0, 24)
        stateLabel.Position = UDim2.new(1, -48, 0.5, -12)
        stateLabel.BackgroundColor3 = toggleState and Color3.fromRGB(60, 180, 60) or Color3.fromRGB(60, 60, 70)
        stateLabel.Text = toggleState and "On" or "Off"
        stateLabel.Font = Enum.Font.GothamBold
        stateLabel.TextSize = 12
        stateLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        stateLabel.Parent = btn
        local stateCorner = Instance.new("UICorner")
        stateCorner.CornerRadius = UDim.new(0, 12)
        stateCorner.Parent = stateLabel

        btn.MouseButton1Click:Connect(function()
            toggleState = not toggleState
            stateLabel.Text = toggleState and "On" or "Off"
            stateLabel.BackgroundColor3 = toggleState and Color3.fromRGB(60, 180, 60) or Color3.fromRGB(60, 60, 70)
            callback(toggleState)
        end)

        btn.MouseEnter:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(48, 48, 54)}):Play()
        end)
        btn.MouseLeave:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(38, 38, 44)}):Play()
        end)

        -- 如果默认状态为 true，立即调用回调
        if toggleState then
            callback(true)
        end

        section.ContentY = section.ContentY + 42
        table.insert(section.Buttons, btn)
        return btn
    end

    table.insert(self.Sections, section)
    return section
end

-- ===================== 核心功能变量 =====================
local autofarm = false
local autoCollectingCubes = false
local autoClaimRewards = false
local farmMoving = false
local showMap = false

-- ===================== Auto Farm 逻辑 =====================
local autoFarmCallback = function(enabled)
    autofarm = enabled
    if not enabled then return end
    coroutine.wrap(function()
        local text = Drawing.new("Text")
        text.Outline = true
        text.OutlineColor = Color3.new(0, 0, 0)
        text.Color = Color3.new(1, 1, 1)
        text.Center = false
        text.Position = Vector2.new(64, 64)
        text.Text = ""
        text.Size = 14
        text.Visible = true

        local startTime = tick()
        local eatTime = 0
        local lastEatTime = tick()
        local timer = 0
        local grabTimer = 0
        local sellDebounce = false
        local sellCount = 0

        local bedrock = Instance.new("Part")
        bedrock.Anchored = true
        bedrock.Size = Vector3.new(2048, 10, 2048)
        bedrock.Position = Vector3.new(0, -5, 0)
        bedrock.BrickColor = BrickColor.Black()
        bedrock.Parent = workspace

        local map = workspace:FindFirstChild("Map")
        local chunks = workspace:FindFirstChild("Chunks")
        if map and chunks then
            map.Parent, chunks.Parent = nil, nil
        end

        local numChunks = 0
        local hum, root, size, events, eat, grab, sell, sendTrack, chunk, radius, autoConn

        local function onCharAdd(char)
            numChunks = 0
            hum = char:WaitForChild("Humanoid")
            root = char:WaitForChild("HumanoidRootPart")
            size = char:WaitForChild("Size")
            events = char:WaitForChild("Events")
            eat = events:WaitForChild("Eat")
            grab = events:WaitForChild("Grab")
            sell = events:WaitForChild("Sell")
            chunk = char:WaitForChild("CurrentChunk")
            sendTrack = char:WaitForChild("SendTrack")
            radius = char:WaitForChild("Radius")

            autoConn = RunService.Heartbeat:Connect(function(dt)
                if not autofarm then
                    autoConn:Disconnect()
                    return
                end

                local ran = tick() - startTime
                local hours = math.floor(ran / 3600)
                local minutes = math.floor(ran / 60) % 60
                local seconds = math.floor(ran) % 60
                local eatMinutes = math.floor(eatTime / 60)
                local eatSeconds = math.floor(eatTime) % 60

                local y = bedrock.Position.Y + bedrock.Size.Y / 2 + hum.HipHeight + root.Size.Y / 2
                local sizeAdd = LocalPlayer.Upgrades.Multiplier.Value / 100
                local addAmount = LocalPlayer.Upgrades.MaxSize.Value / sizeAdd
                local sellTime = addAmount / 2
                local sellMinutes = math.floor(sellTime / 60)
                local sellSeconds = math.floor(sellTime) % 60
                local secondEarn = math.floor(((LocalPlayer.Upgrades.MaxSize.Value + 0.5) ^ 2 - 0.25) / 2 * 100 / sellTime)
                local minuteEarn = secondEarn * 60
                local hourEarn = minuteEarn * 60
                local dayEarn = hourEarn * 24

                text.Text = "\nRuntime: " .. string.format("%ih%im%is", hours, minutes, seconds) ..
                            "\nActual time: " .. string.format("%im%is", eatMinutes, eatSeconds) ..
                            "\nApprox time: " .. string.format("%im%is", sellMinutes, sellSeconds) ..
                            "\nPer day: " .. dayEarn ..
                            "\nChunks: " .. numChunks

                hum:ChangeState(Enum.HumanoidStateType.Physics)
                grab:FireServer()
                root.Anchored = false
                eat:FireServer()
                sendTrack:FireServer()

                if chunk.Value then
                    if timer > 0 then numChunks += 1 end
                    timer = 0
                    grabTimer += dt
                else
                    timer += dt
                    grabTimer = 0
                end

                if timer > 60 then
                    hum.Health = 0
                    timer = 0
                    numChunks = 0
                end

                if grabTimer > 15 then
                    size.Value = LocalPlayer.Upgrades.MaxSize.Value
                end

                if (size.Value >= LocalPlayer.Upgrades.MaxSize.Value) or timer > 8 then
                    if timer < 8 then
                        sell:FireServer()
                        if not sellDebounce then
                            local args = {{MapTime = -1, Paused = true}}
                            Events.SetServerSettings:FireServer(unpack(args))
                        end
                        sellDebounce = true
                    else
                        local args = {{MapTime = -1, Paused = true}}
                        Events.SetServerSettings:FireServer(unpack(args))
                    end
                    numChunks = 0
                elseif size.Value == 0 then
                    if sellDebounce then
                        local currentEatTime = tick()
                        eatTime = currentEatTime - lastEatTime
                        lastEatTime = currentEatTime
                        sellCount += 1
                    end
                    sellDebounce = false
                end

                if farmMoving then
                    local bound = 300
                    local startPos = CFrame.new(-bound/2, y, -bound/2)
                    local r = radius.Value * 1.1
                    local dist = (r * numChunks)
                    local x = dist % bound
                    local z = math.floor(dist / bound) * r
                    local offset = CFrame.new(x, 0, z + r * 2)
                    if z > bound then
                        local args = {{MapTime = -1, Paused = true}}
                        Events.SetServerSettings:FireServer(unpack(args))
                        numChunks = 0
                    end
                    root.CFrame = startPos * offset
                else
                    root.CFrame = CFrame.new(0, y, 0)
                end
            end)

            hum.Died:Connect(function()
                if autoConn then autoConn:Disconnect() end
                local args = {{MapTime = -1, Paused = true}}
                Events.SetServerSettings:FireServer(unpack(args))
            end)

            char:WaitForChild("LocalChunkManager").Enabled = false
            char:WaitForChild("Animate").Enabled = false
        end

        if LocalPlayer.Character then
            task.spawn(onCharAdd, LocalPlayer.Character)
        else
            LocalPlayer.CharacterAdded:Wait()
            task.spawn(onCharAdd, LocalPlayer.Character)
        end

        local charAddConn = LocalPlayer.CharacterAdded:Connect(onCharAdd)

        while autofarm do
            task.wait()
            if workspace:FindFirstChild("Loading") then
                workspace.Loading:Destroy()
            end
            if map and chunks then
                if showMap then
                    map.Parent, chunks.Parent = workspace, workspace
                else
                    map.Parent, chunks.Parent = nil, nil
                end
            end
        end

        charAddConn:Disconnect()
        if autoConn then autoConn:Disconnect() end
        if map and chunks then
            map.Parent, chunks.Parent = workspace, workspace
        end
        if hum then
            hum:ChangeState(Enum.HumanoidStateType.GettingUp)
        end
        bedrock:Destroy()
        text:Destroy()
    end)()
end

-- ===================== Auto Collect 逻辑 =====================
local autoCollectCallback = function(enabled)
    autoCollectingCubes = enabled
    if not enabled then
        LocalPlayer.PlayerScripts.CubeVis.Enabled = true
        return
    end
    coroutine.wrap(function()
        LocalPlayer.PlayerScripts.CubeVis.Enabled = false
        while autoCollectingCubes do
            task.wait()
            local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if root then
                for _, v in ipairs(workspace:GetChildren()) do
                    if v.Name == "Cube" and v:FindFirstChild("Owner") and (v.Owner.Value == LocalPlayer.Name or v.Owner.Value == "") then
                        v.CFrame = root.CFrame
                    end
                end
            end
        end
        LocalPlayer.PlayerScripts.CubeVis.Enabled = true
    end)()
end

-- ===================== Auto Claim 逻辑 =====================
local autoClaimCallback = function(enabled)
    autoClaimRewards = enabled
    if not enabled then return end
    coroutine.wrap(function()
        while autoClaimRewards do
            task.wait(1)
            for _, reward in ipairs(LocalPlayer.TimedRewards:GetChildren()) do
                if reward.Value > 0 then
                    Events.RewardEvent:FireServer(reward)
                end
            end
            Events.SpinEvent:FireServer()
        end
    end)()
end

-- ===================== UI初始化 =====================
local mainUI = UI:NewWindow("Eat the World")

local autoSection = mainUI:NewSection("Auto Features")

-- 默认状态设为 true，UI 会立即显示 "On" 并启动功能
autoSection:CreateToggle("Auto farm", autoFarmCallback, true)
autoSection:CreateToggle("Auto collect", autoCollectCallback, true)
autoSection:CreateToggle("Auto claim", autoClaimCallback, true)

print("[Eat the World] Script loaded - Auto Farm, Auto Collect & Auto Claim are ENABLED by default")
